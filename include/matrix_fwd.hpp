#pragma once
// Forward declarations
#include <cstddef>

template <size_t N> struct MatrixSlice;

template <typename T, size_t N> class Matrix;
template <typename T, size_t N> class MatrixRef;

struct slice;
